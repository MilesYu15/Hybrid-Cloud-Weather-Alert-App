apiVersion: v1
kind: Pod
metadata:
  name: kafka-test
  namespace: default
spec:
  containers:
  - name: kafka-test
    image: confluentinc/cp-enterprise-kafka:5.5.0
    command:
      - sh
      - -c
      - "exec tail -f /dev/null"